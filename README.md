# Tabulky

Simple spreadsheets, which support realtime editing and are phone friendly.

## Setup

```sh
pnpm i

pnpm dev # Local frontend dev server
node --watch server/index.ts # Local backend server
```


## Deploy

```
docker compose up -d
```
